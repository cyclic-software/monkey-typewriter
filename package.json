{
  "name": "monkey-typewriter-dev",
  "version": "1.0.0",
  "description": "Randomly generated words and slugs that look and sound like english words",
  "main": "demo/index.js",
  "scripts": {
    "p": "cd src && npm version patch && cd .. && rm -rf ./dist && mkdir -p dist && cp src/* dist/ && cp README.md dist && cd dist &&  npm publish"
  },
  "keywords": [
    "random",
    "faker",
    "language",
    "slug",
    "slugify",
    "word",
    "lorem",
    "unique",
    "ipsum"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^9.1.3"
  }
}
